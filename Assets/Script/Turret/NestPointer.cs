using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NestPointer : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        transform.Rotate(0, 40f * Time.deltaTime, 0);
    }
}
